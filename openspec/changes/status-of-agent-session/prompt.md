# Prompt

The current status of agent sessions is always "running", but we should differentiate between "Agent is working" and "Action required". Maybe "Running - Agent working" and "Running - User Input required" or so.
