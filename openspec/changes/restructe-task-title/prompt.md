# Prompt

The console buttons are next to the title, which let it wrap across multiple lines. Make sure the title stays on top and the console buttons are in the next line.

also the "show details" button should have the same size as the "action" button next to it.
