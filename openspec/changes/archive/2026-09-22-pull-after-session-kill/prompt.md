# Prompt

After a change is done and I close the console it asks to clean the work tree at this point we should also ask to do a git pull, so the local environment retrieves the changes and we are not having a wrong view.
