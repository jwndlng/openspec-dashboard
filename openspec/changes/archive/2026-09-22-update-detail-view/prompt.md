# Prompt

Remove the "Copy apply" button and replace it with "Show details" - So clicking only there should show the details. Also the detail page is not very nice.

I would prefer if it looks more like an overlay. We don't need all the current status. It's sufficient if we have the links like Proposa, Specs, Design and Tasks and we can scroll through those inputs. If we want to see the labels etc we go back to the board
