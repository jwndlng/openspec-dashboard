# Prompt

The label coloring in a task must be changed. So running should be blue, branch names should be orange like uncommited. In general we should use different coloring and also not mix with the project coloring
