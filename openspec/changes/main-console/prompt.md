# Prompt

Implement a main console that is independent from the projects. An icon on the top right next to the theme switch that will just open a local agent console, so it can be used to spin off new tasks or do general tasks that are not task related.
